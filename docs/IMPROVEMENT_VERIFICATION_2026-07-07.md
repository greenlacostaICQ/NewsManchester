# Проверка доработок 0001–0057 на реальных данных — 2026-07-07

Проверено по **реально отправленному дайджесту** `data/outgoing/current_digest.html` (Greater Manchester Brief 2026-07-02, 08:24),
отчётам прогона `20260702T080137` в `data/state/*.json` и полному CI-сьюту (`unittest discover`, сегодня: **815 тестов, 1 fail**).

Провенанс: последний реальный прогон — 08:29 Jul 2. Пакет 0039–0053 закоммичен позже (10:50–13:37) и **в проде не прогонялся** → в отправленном дайджесте их проблемы ещё видны (это и есть «как по сути» для них — не «не проверено», а **прямо в HTML лежит дефект**).

---

## A. Реальные дефекты в отправленном дайджесте 2026-07-02 (это факты из HTML, не из отчётов)

| # | Дефект | Где в HTML | Какая доработка должна была поймать |
|---|---|---|---|
| D1 | **Дубль одной истории в «Свежие новости»**: Шабир Ахмед / депортация лидера Rochdale — строка 12 (About Manchester) и строка 14 (BBC/Бернхэм). Два разных источника, одна новость, обе в эфире. | стр. 12 + 14 | 0030, 0038, cross-section dedup |
| D2 | **Лид не в блоке лида.** «Главная история дня» пуст (`lead_visible=false`); при этом сама лид-новость (Бернхэм) ушла обычной строкой в «Свежие». | нет блока лида | 0006, 0012/S5 |
| D3 | **Лифты в эфире.** Firswood (стр. 22) и Queens Road (стр. 24) — алерты про лифт без влияния на движение. | стр. 22, 24 | 0045 |
| D4 | **«Еда, открытия и рынки» пустая** (0 из мин. 3). | стр. 55–56 | 0001, 0053 |
| D5 | **Штамп-концовки** «Следите за обновлениями команды» (футбол 32–34), «сверьте детали и условия перед поездкой» (выходные 38, 40, 41). | стр. 32–34, 38, 40, 41 | 0041, 0044 |
| D6 | **«Выходные в GM» = 6 из 128 кандидатов**; формат «4 июля — Cheadle Makers Market: рынок в Чидле…» шаблонный; La Prairie beauty-brunch (ретейл, без venue) попал в выходные. | стр. 36–42 | 0053, 0026 |
| D7 | **«Дальние анонсы» = 1 строка, и это фестиваль в Ньюкасле** (Lost Minds Festival, Exhibition Park Newcastle) — не GM. Writer насчитал 11, в HTML 1. | стр. 65–66 | 0032, 0010/0014 (гео) |
| D8 | **next_7_days содержит обеденный концерт** (Denise Morgan «Live at Lunchtime», стр. 52) и гиг (Antarctic Monkeys, стр. 53) — должны быть в Ticket Radar/выходных. | стр. 52–53 | 0052 |
| D9 | **Секция называется «Бесплатные business/tech события для тебя»**, но внутри платные/unknown события. | стр. 60 | 0048 |
| D10 | **Судья качества не отработал** (`429, Requested 43721 > 30000`), выпуск ушёл без финальной проверки. | — | 0039, 0040, 0021 |
| D11 | **CI-сьют красный**: `test_dated_weekend_event_is_budget_exempt` (хардкод даты 2026-07-04). | тест | 0026/0057 (time-bomb) |

Дополнительно из отчётов: `dedupe_lost_event=140`, `final_loss_check.dedupe_dropped=476`, `critical_losses=261` — дедуп **одновременно** теряет много событий И пропускает дубль D1 (over-merge в одном месте, under-merge в другом).

---

## B. Таблица по каждой доработке

Колонки: **Зачем** (проблема) · **Как задумано** · **Как по сути** (реальные данные) · **Чинить**.
Статус: ✅ работает · 🟡 частично · ❌ не работает · 🟠 не в проде (код+тесты есть, дефект в HTML остался) · 🧊 диагноз.

| # | Доработка | Зачем | Как задумано | Как по сути (реальные данные) | Что чинить |
|---|---|---|---|---|---|
| 0001 | Единый пул резерва для бэкфилла | Свежие/Радар выходили ниже минимума, резерв не виден | Добор из единого пула до floor | 🟡 Свежие 8✅, но **Радар 2/5, Еда 0/3** (`no_recoverable_reserve_with_facts`) | Разобрать пустой резерв Еды/Радара — мёртвый парсер (D4) + слишком строгий отсев |
| 0002 | Обогащать, не дропать selected без строки | 92 карточки теряли `draft_line` | Refetch→rewrite, иначе детерминир. строка | ✅ `missing_draft_line=0` в rewrite_inventory | — |
| 0003 | Backoff/limiter финального редактора | Редактор падал по 429, 68 строк без вычитки | Пачки через token-bucket, retry | ✅ round1 ok, 60/60 строк, `failed_batches=0` | — |
| 0004 | Кэш промптов | Кэш не использовался | Реюз по content-hash | ✅ косвенно (cost/history пишутся); отдельной метрики нет | добавить hit-rate в отчёт |
| 0005 | «Выходные» видят события, а не листинги | Источник отдавал страницы-списки | next_occurrence в pre-validation | ✅ секция непустая (6), но см. 0053/D6 | см. 0053 |
| 0006 | Лид виден в HTML | Лид исчезал из видимого HTML | Global lead arbitration + защита от dedup | ❌ **`lead_visible=false`** (D2); лид ушёл строкой в Свежие | Почему curator-lead не рендерится в блок лида — трейс `lost_leads`→writer |
| 0007 | LLM CV-match на профсобытиях | 25/42 не доходили до модели | Все eligible → в CV | 🟠 адресовано 0046 (не в проде); см. 0046 | прод-прогон |
| 0008 | «84 источника 0» = фильтрация | Ложный вывод о мёртвых источниках | Метрика вклада | ✅ `source_status`: 78 fetched-0, не dead | — |
| 0009 | «Выходные» ломают 3 механизма | Уточнение к 0005 | — | 🧊 диагноз → 0025/0026/0053 | — |
| 0010 | London-площадки не в GM-радар | Гео-утечка билетов | venue-scope резолвер | 🟡 Ticket Radar чист, **но Ньюкасл-фест в «Дальних»** (D7) | гео-гейт на «Дальние анонсы»/«вне GM» |
| 0011 | Гейты меряют HTML, не writer_report | Проверки видели writer, не HTML | Контроль на shipped HTML | ✅ `control_assertion` ловит расхождения (writer 11 vs html 1 и т.д.) | — (детектор работает; чинить надо то, что он ловит) |
| 0012 | Волна 1 (S1–S5) | Пакет базовых фиксов | S1 пул, S2 редактор, S3 backstop, S4 HTML-контракт, S5 лид | 🟡 S1–S4 живут; **S5 (лид) не держит** (D2) | см. 0006 |
| 0013 | Канонический модуль фактов события | Дата/скоуп считались в разных местах | Один корень date/range/confidence | ✅ `event_completeness` по единой схеме | — |
| 0014 | venue-scope резолвер (гео) | London-события в GM | Гео-класс площадки | ✅ билеты GM-чистые | распространить на «Дальние» (D7) |
| 0015 | Транспорт = контракт пасс. эффекта | Пустой транспорт-блок | Только реальные сбои движения | ✅ `transport_coverage=disruptions_rendered`, 8/8 | но лифты просочились → см. 0045 |
| 0016 | Классификатор рус-блока | Ложная фильтрация рус-событий | Позитив-evidence | 🟡 1 строка (опера Онегин, Хэмпшир) — «рус», но не концерт/стендап | сузить/расширить скоуп секции |
| 0017 | pre-writer balance + payload cap | Дисбаланс до writer | Scope-gated cap + ticket inventory | ✅ board/budget считаются | — |
| 0018 | on-sale окно читает дату события | Окно билетов от даты события | Парс on-sale из листинга | ✅ (код+тесты) | — |
| 0019 | eligible держатся (held) | Пограничные молча дропались | held, не drop | ✅ `borderline_queue=85 held` | — |
| 0020 | Метрика zero_contribution_by_stage | Непонятно где теряются источники | Атрибуция по стадии | ✅ `source_health.counts`+funnel заполнены | — |
| 0021 | Pre-send repair executor | Судья видел брак, но не чинил HTML | Ремонт→замена→честное снятие | 🟠 `repair_executor=null` (судья упал 429 до ремонта, D10) | зависит от 0039/0040 |
| 0022 | Hard fact-lock редактора | Редактор мог вписать новый факт | Отклонять токен без evidence | ✅ `fact_lock_rejected=1` | — |
| 0023 | Deep enrichment афиш/профсобытий | Слабый summary без фактов | Fetch дочерней страницы | ✅ venue/date дотягиваются | — |
| 0024 | Финальный per-block отчёт | Не было единой таблицы выбора | `final_selection_report.json` | ✅ 15 секций, totals, category_health | — |
| 0025 | Восстановление досуга и транспорта | Массовый отсев досуга, пустой транспорт | Дата в pre-validation, reroute off | ✅ транспорт 8 (было 0) | досуг всё ещё тонкий (D6) |
| 0026 | E2-фикс + бюджет выходных | Датированные Ai Weiwei/Crossroad дропались | `has_specific_date→return False`, exempt | ✅ Ai Weiwei в Радаре (стр. 46); **но тест-time-bomb D11** | починить фикстуру теста (D11) |
| 0027 | Workflow timeout 35 мин | Прогон отменялся по таймауту | +10 мин запаса | 🟠 config-only, прогоном не проверено | — |
| 0028 | Cancel-proof observability | При отмене не было артефактов | Flush snapshot/timings | 🟡 snapshot/speed пишутся; cancel-drill не гонялся | — |
| 0029 | Targeted editor round 2 | Round 2 слал весь выпуск | Только не-ok/uncovered/sensitive | 🟠 не triggered (round1 хватило) | — |
| 0030 | show=renderable + строгий Fresh dedupe | show без строки; склейка разных crime | Строже incident-dedup | ❌ **под-склейка**: дубль Шабира прошёл (D1); при этом `dedupe_lost_event=140` (пере-склейка) | Пересобрать incident-dedup: same-person+same-event должны мержиться; калибровать anchor |
| 0031 | No weak fallback + prevalidated reserve | title-only填 hard news | Запрет слабого fallback | ✅ underflow честный, без мусорного填 | — |
| 0032 | future_announcements = A-tier | A-tier анонс уходил в manual | Признать блок A-tier | 🟡 распознан, но **writer 11 → HTML 1** (D7), 1 из них Ньюкасл | разобрать потерю на рендере «Дальних» |
| 0033 | Пустой «Что важно» не блокирует | Пустой блок ронял весь выпуск | warning при честном shortfall | ✅ блок 3/4 непустой, механизм на месте | — |
| 0034 | Today Focus добирает practical reserve | Блок 0/3 при живом M60-кандидате | Квота + recovery из last_24h/city_watch | ✅ rendered 4, moved fresh 1 + city_watch 2 | — |
| 0035 | Category health + conservation + cache | Нет honest per-category verdict | health-verdict + no-loss check | ✅ `conserved=true` (1416→1417), category_health есть | — |
| 0036 | Inventory-слой + night waves | Нет проверяемого inventory | Schema-versioned карточки, upsert | ✅ `inventory/*.jsonl` пишутся; но морнинг из них НЕ кормится | подключить morning-read (пункт 15) |
| 0037 | Night inventory → GitHub Actions | launchd — не прод-путь | Workflow + concurrency | ✅→заменён 0054 | — |
| 0038 | Repeat-policy: food/openings | Ресторан повторялся как билет | Typed carry-over + visible quarantine | 🟡 exact-fingerprint ловит (`visible_repeat_rejected=10`), **но семантику — нет** (D1); `vector_or_semantic_matches=56` только считаются | добавить семантический visible-quarantine (порог) |
| 0039 | Судья Map/Reduce | 429 на монолите 43k | Чанки+limiter+reduce | 🟠 **не в проде: отчёт `429, 43721>30000`** (D10); фикс есть, офлайн 14 чанков/4509 | прод-прогон |
| 0040 | Детерминированная сеть судьи | При падении судьи — ноль проверки | Локальный чек без LLM | 🟠 `deterministic_post_check=null` в отчёте (D10) | прод-прогон |
| 0041 | Обогащать короткие, не добивать | Штамп-концовки вместо факта | Controlled recovery | 🟠 **не в проде: штампы в HTML** (D5) | прод-прогон |
| 0042 | Кэш перевода не отдаёт короткое | Футбол из кэша 1 предложением | Гейт контракта блока | 🟠 не в проде (код+тесты) | прод-прогон |
| 0043 | Полнота перевода (пропажа факта) | Терялась severity-деталь | Обратный fact-lock | 🟠 не в проде; модуль+13 тестов ✅ | прод-прогон |
| 0044 | Правило пустых концовок + post-check | Штамп «по теме» не снимался | Промпт + deterministic post-check | 🟠 **не в проде: поля `empty_ending_post_check` нет**, штампы в HTML (D5) | прод-прогон |
| 0045 | Лифты/эскалаторы не публиковать | Firswood/Queens Road в эфире | drop в validator без movement | 🟠→✅ **модуль на реальных: обе карточки WOULD_DROP=True**; но в проде — в HTML (D3) | прод-прогон (код верный) |
| 0046 | Профсобытия: матч смыслом | 25/42 не доходили до CV | Stage1 eligibility → Stage2 CV все | 🟠→✅ **модуль: 16/42 eligible на реальных**; CV-вердикт нужен прод | прод-прогон |
| 0047 | Реальное обоснование модели | `why` шаблонный у всех | reason→why→шаблон | 🟠 не в проде | прод-прогон |
| 0048 | Убрать требование бесплатности | 2/3 показанных unknown в «Бесплатных» | Переименовать + access-label | 🟠 **не в проде: секция всё ещё «Бесплатные…»** (D9) | прод-прогон |
| 0049 | Согласовать гейты профсобытий | go выпал, consider/reject прошёл | skip невидим, go>consider, cap внутри | 🟠 не в проде | прод-прогон |
| 0050 | Валидация URL (HTML в href) | CONEXEN href с `<p><strong>` | `valid_http_url` + санитайз | 🟠→✅ **модуль: битый CONEXEN→False, норм с `&#`→True**; битый href был в draft | прод-прогон (код верный) |
| 0051 | Единая шкала ранжирования | keyword-100 забивал верх доски | score provenance + своя шкала | 🟠 не в проде | прод-прогон |
| 0052 | Контракт маршрутизации блоков | car boot/обед. концерт в «7 дней» | leisure не остаётся в next_7 | 🟠 **не в проде: `final_section_routing` all-0**, обед. концерт в next_7 (D8) | прод-прогон |
| 0053 | Weekend Inventory защищён | 6 из 128 weekend теряются | never-drop на caps, weekly→дата | 🟠 **не в проде: `weekend_inventory_loss_trace` ОТСУТСТВУЕТ**, секция 6/128 (D6) | прод-прогон |
| 0054 | Night inventory через cron-job.org | GitHub schedule лагал за 08:00 | 5 волн cron-job.org Europe/London | 🟡 test-run 204; inventory пишется. Полную ночь свериться `gh run list` | проверить ночной прогон |
| 0055 | Фикс 8 регрессий пакета | CI был красный (8 регрессий) | Порядок гейтов + fallback | ✅ 8 устранены; но см. 0057/D11 | — |
| 0056 | .gitattributes union merge | Гонка за inventory_run_log | merge=union + retry | ✅ `.gitattributes` в репо | — |
| 0057 | Остаток 7 pre-existing → 0 fail | Time-bomb notability + skiddle | checked_at=now-1d, allow-list | ❌ **устарел: сейчас 1 fail** (новый time-bomb 2026-07-04, D11) | починить `test_dated_weekend_event_is_budget_exempt` |

---

## C. Что чинить (приоритет)

1. **CI красный (D11, 0057).** `tests/test_publish_plan_contract.py:207` хардкодит `date_start:"2026-07-04"`. Заменить на вычисляемую ближайшую субботу (`now_london()+timedelta(days=(5-wd)%7)`). ≤3 строки. — *могу сделать сразу.*
2. **Повторы D1 (0030/0038).** Семантический дубль одной истории проходит, т.к. visible-quarantine только по exact-fingerprint, а `semantic_embedding` (56 матчей) не действует на видимые строки. Нужен порог semantic-quarantine для одной секции + пересбор incident-dedup (Шабир Ахмед: same person+event должны мержиться). Параллельно `dedupe_lost_event=140` — калибровать, чтобы не пере-склеивать.
3. **Лид D2 (0006/S5).** Curator-lead не попадает в блок «Главная история дня», рендерится обычной строкой. Трейс `lost_leads`→точка в writer, где лид теряет lead-роль.
4. **Прод-прогон для 0039–0053 (D3, D5, D8, D9, D10).** 15 доработок с кодом+зелёными тестами, но их дефекты **прямо в отправленном HTML** (лифты, штампы, обед.концерт в 7 днях, «Бесплатные», 429). Один свежий прогон закроет их и даст цифры. Модульно уже подтвердил, что 0045/0050/0046 отрабатывают на реальных входах.
5. **Еда/Радар пустые (D4, 0001).** Мёртвый парсер `Great Northern Makers Market` + строгий отсев — резерв пуст. Проверить парсеры food/markets.
6. **Гео «Дальних анонсов» (D7, 0010/0032).** Ньюкасл-фестиваль в GM-дайджесте; и потеря writer 11→HTML 1 на рендере.
